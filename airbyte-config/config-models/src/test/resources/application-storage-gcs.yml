airbyte:
  cloud:
    storage:
      type: gcs
      bucket:
        log: log
        state: state
        workload-output: workload
        activity-payload: payload
      gcs:
        application-credentials: credz

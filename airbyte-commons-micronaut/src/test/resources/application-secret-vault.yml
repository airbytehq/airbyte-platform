airbyte:
  secret:
    persistence: vault
    store:
      vault:
        address: https://vault:6000
        prefix: /test-secrets
        token: opensesame
        token-ref-name: test-token-ref-name
        token-ref-key: test-token-ref-key

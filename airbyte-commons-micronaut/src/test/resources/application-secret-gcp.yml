airbyte:
  secret:
    persistence: google_secret_manager
    store:
      gcp:
        project-id: test-project-id
        credentials: test-credentials
        region: us-central1
        credentials-ref-name: test-credentials-ref-name
        credentials-ref-key: test-credentials-ref-key

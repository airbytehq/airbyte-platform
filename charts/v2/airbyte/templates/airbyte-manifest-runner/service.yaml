{{- if .Values.manifestRunner.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name }}-airbyte-manifest-runner-svc
  {{- with .Values.manifestRunner.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "airbyte.labels" . | nindent 4 }}
spec:
  type: {{ .Values.manifestRunner.service.type }}
  ports:
    - port: {{ .Values.manifestRunner.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airbyte.selectorLabels" . | nindent 4 }}
{{- end }}